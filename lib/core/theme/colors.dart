import 'package:flutter/material.dart';

class AppPalette {
  // Primary brand colors (Ahadu Bank style: Deep Red / Burgundy)
  static const Color primary = Color(0xFF900B29);
  static const Color primaryLight = Color(0xFFC72445);
  static const Color primaryDark = Color(0xFF5A0013);

  // Accents
  static const Color accent = Color(0xFFD4AF37); // Gold accent often used in banking

  // Neutrals
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF757575);
  static const Color divider = Color(0xFFEEEEEE);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color successBg = Color(0xFFE8F5E9);
  static const Color error = Color(0xFFE53935);
  static const Color errorBg = Color(0xFFFFEBEE);
  static const Color warning = Color(0xFFFFB300);
  static const Color info = Color(0xFF2196F3);
}
